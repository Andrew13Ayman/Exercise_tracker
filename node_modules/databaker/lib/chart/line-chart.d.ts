import { LineChartOptions } from "../interfaces/chart";
import { Image } from "../interfaces/image";
export declare class LineChart {
    options: LineChartOptions;
    constructor(options?: Partial<LineChartOptions>);
    draw(labels: string[], datas: number[][]): Image;
}
