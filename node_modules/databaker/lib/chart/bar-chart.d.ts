import { BarChartOptions } from "../interfaces/chart";
import { Image } from "../interfaces/image";
export declare class BarChart {
    options: BarChartOptions;
    constructor(options?: Partial<BarChartOptions>);
    draw(labels: string[], datas: number[][]): Image;
}
