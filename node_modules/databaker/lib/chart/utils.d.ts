import { ScaleBand, ScaleLinear } from "d3-scale";
export declare function drawAxis(context: CanvasRenderingContext2D, width: number, height: number, x: ScaleBand<string>, y: ScaleLinear<number, number>, options: {
    borderColor: string;
    textColor: string;
}): void;
